# HotelStay+ (Rooms & Restaurant Reservations) — CRA (no Vite)

React (JSX in `.js` files) + Node/Express API with in-memory data.

## Run Backend
cd server
npm install
npm start
# http://localhost:4000

## Run Frontend (CRA)
cd ../client
npm install
npm start
# http://localhost:3000

### Demo credentials
- Admin: `admin@hotel.com` / `Admin@123`
- Or sign up a new customer.
